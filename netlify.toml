[build]
  command = "python3 pipeline/13-ssg/build.py --clean"
  publish = "pipeline/13-static-site"

[build.environment]
  PYTHON_VERSION = "3.10"
  # Adicionar chaves de API se necessário via Netlify UI
