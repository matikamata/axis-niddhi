name: Deploy DhammaSeed para Lighthouse IPFS

on:
  push:
    branches:
      - main # (Se o seu branch principal chamar 'master', mude aqui)

jobs:
  deploy-to-ipfs:
    runs-on: ubuntu-latest
    steps:
      - name: 🧘‍♂️ Meditação Inicial (Checkout do Repositório)
        uses: actions/checkout@v4

      - name: 📦 Preparando o Solo (Instalando Node.js)
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: 🚀 Invocando o Farol (Instalando Lighthouse SDK)
        run: npm install -g @lighthouse-web3/sdk

      - name: 🌌 Fixando a Joia no IPFS (Upload para Svalbard)
        run: |
          echo "Guardando a chave no coração do monge-assistente..."
          lighthouse-web3 api-key --import ${{ secrets.LIGHTHOUSE_API_KEY }}
          echo "Iniciando a oferenda ao cosmos..."
          lighthouse-web3 upload pipeline/13-static-site
