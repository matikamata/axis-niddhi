# Branch Protection Smoke Test

This temporary document verifies that main branch protection requires PR-based changes.

Expected:
- no direct push to main;
- branch push works;
- PR merge works;
- main receives the change only after PR merge.
